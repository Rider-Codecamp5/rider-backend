const express = require('express');
const router = express.Router();
// const passengerController = require('../controllers/Passenger')

// router.get('/passenger', passengerController)

module.exports = router;