const express = require("express");
const router = express.Router();
// const creditCardsController = require('../controllers/CreditCards')

// router.get('/credit-cards', creditCardsController) 

module.exports = router;